改动2：
         rte_platform.h：
         之前改漏的地方，rte_platform_device当中新加入继承rte_device结构体
         新增宏定义RTE_DEV_TO_PLATFORM,对应于RTE_DEV_TO_PCI,在驱动实现时由rte_eth_dev结构获取rte_platform_device。
         新增宏定义RTE_PMD_REGISTER_PLATFORM


hns.ethdev.h对应新增宏定义HNS_DEV_TO_PCI，用于获取rte_platform_device

hns.ethdev.c中：
         eth_hns_dev_init、uninit初始化和驱动注册方面的修改

rte_ethdev.h上次漏加的：
     eth_rx_burst_remain_t;
     eth_tx_burst_remain_t;